# Telegram Bot Configuration
BOT_TOKEN=your-telegram-bot-token-here
HOW_TO_ORDER_VIDEO_ID=your-telegram-file-id-here
SUPPORT_LINK=@LazR_Supports

# Brand Name (Optional) — lets this codebase be redeployed ("cloned") for a
# different business by only changing env vars. If unset, defaults to Blessing.Kh.
BRAND_NAME=Your Brand Name
BRAND_NAME_UPPER=YOUR BRAND SMM

# Channel & Admin Notification Group (Optional)
CHANNEL_LINK=https://t.me/your_public_channel
GROUP_CHAT_ID=your-admin-notification-chat-id

# Problem-Solve Ticket Group (Optional) — where "❓ Other Reason" order
# tickets get posted, separate from GROUP_CHAT_ID above. Usually set live via
# /setproblemgroup run inside that group (persists to Supabase bot_settings),
# so this env var is only needed if you'd rather pin it at deploy time.
# PROBLEM_SOLVE_GROUP_ID=your-problem-solve-group-chat-id

# Payment Merchant Config (Optional)
BAKONG_ACCOUNT_ID=your_bakong_account@bank
BAKONG_TOKEN=your-bakong-open-api-token
ACLEDA_MERCHANT_ID=your_acleda_merchant@aclb
ACLEDA_API_TOKEN=your-acleda-api-token
PAYWAY_LINK=https://link.payway.com.kh/your-merchant-link

# Wing Bank KHQR API (Optional — Mode 5). Unlike Bakong's Open API, Wing's
# OnlineSDK-KHQR has no daily request-limit tier and pushes a webhook on
# successful payment. Obtained from openapi.wingbank.com.kh after
# submitting Wing's Corporate Partner Checklist documents. Can also be set
# live via Admin Menu > Bot Settings > Edit Wing Credentials (in-memory
# only — set here too for it to survive a redeploy).
WING_CLIENT_ID=your-wing-client-id
WING_CLIENT_SECRET=your-wing-client-secret
# WING_API_BASE=https://openapi.wingbank.com.kh  # override only if Wing gives a different base URL

# khqr.cc (KHQRPay) — Optional, Mode 6. A third-party KHQR gateway (NOT
# NBC's own Bakong Open API) — plain SHA1/SHA256 keyed hashes, no encryption.
# Get the Secret Key from khqr.cc Dashboard > API Keys. Also set khqr.cc's
# "Global Webhook Endpoint" (dashboard > Integrations) to
# <your-domain>/khqrcc-webhook for auto-payment to work.
KHQR_CC_SECRET=your-khqr-cc-secret-key
# KHQR_CC_ENDPOINT=https://khqr.cc/api/<your-profile-id>/payment-gateway/v1/payments/qr-api-khqrcc  # ABA Pay variant — see khqr.cc "ABA PAY (KHQRCC)" docs, not the plain "Direct QR API"

# WebApp Portal URL (Optional — the deployed website/ WebApp for this instance)
WEB_APP_URL=https://your-app.onrender.com

# Supabase Database Configuration (Optional - falls back to local memory if empty)
SUPABASE_URL=https://your-supabase-project-ref.supabase.co
SUPABASE_KEY=your-supabase-anon-key-here

# Admin Access (Optional - comma-separated Telegram numeric user IDs)
# If unset, the bot falls back to its built-in default admin IDs.
# Set this to take exclusive control of who has admin access.
ADMIN_IDS=your-telegram-user-id-here

# Host Support Access (Optional - comma-separated Telegram numeric user IDs)
# Only relevant for white-label clones (see NEW_CLIENT_SETUP.md): the hosting
# provider's own ID(s), kept as admin on every clone they host for support/billing.
# Leave unset for a self-owned deployment.
HOST_SUPER_ADMIN_ID=
